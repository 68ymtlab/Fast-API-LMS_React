# Pure PostgreSQL (Supabase 非依存)

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ymtlab68_lms
POSTGRES_DB=lms
POSTGRES_PORT=5432

# Backend 接続用 (backend/.env で使用)
# DATABASE_URL=postgresql+asyncpg://postgres:CHANGE_ME_POSTGRES_PASSWORD@db:5432/lms
